;;; early-init.el --- Emacs config for dodgez

;;; Commentary:
;; This is the early init file for dodgez's Emacs config.

;;; Code:
(set-face-attribute 'default nil :font "Hack Nerd Font Mono" :height 140)

(provide 'early-init)
;;; early-init.el ends here
